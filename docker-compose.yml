networks:
  proxy_sql-network:
    external: true
services:
  tetris-service:
    image: nginx:alpine
    container_name: tetris
    hostname: tetris
    networks:
      - proxy_sql-network
    volumes:
      - /src/tetris/web:/usr/share/nginx/html:ro
    restart: unless-stopped
